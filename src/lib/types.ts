interface LinkType {
  name: string;
  path: string;
}

interface SliderConfigType {
  cardGap: number;
  cardWidth: number;
  smWindow?: number;
  smCardWidth?: number;
  cardNo: number;
}

export type { LinkType, SliderConfigType };
